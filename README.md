# Как обновить портфолио с помощью update.exe (просто для новичков) 🚀

**Standalone EXE: не требует установки Python! Просто дважды кликните.**

## Что делает эта программа?
Программа берёт готовый шаблон сайта `index.html` и заполняет его **вашими** данными:
- Ваше имя, фото, текст "Обо мне".
- Навыки (сколько процентов знаете HTML, CSS и т.д.).
- Числа: сколько проектов сделал, клиентов, наград.
- **Ваши проекты**: фото, названия, описания, категории (веб, приложения, дизайн), ссылки.
- Контакты: адрес, email, телефон.
- Ссылки: VK, Telegram, GitHub.

В конце создаёт новый файл `update_index.html` – откройте в браузере (Chrome, Edge), и ваше портфолио готово! ✅

## Что нужно заранее?
1. Файл `index.html` (шаблон портфолио) должен быть в той же папке, что и `update.exe`.
2. Создайте папку `images` рядом с `update.exe`.
3. Положите в `images` свои фото:
   - `my_photo.jpg` – ваше фото (квадратное, 300x300 пикселей лучше).
   - `project_1.jpg`, `project_2.jpg` и т.д. – скриншоты проектов.

**Как создать папку?** Правой кнопкой мыши → "Создать" → "Папку" → назовите `images`.

## Пошаговая инструкция (даже если никогда не программировал)
1. **Запустите программу:**
   - Положите `update.exe`, `index.html` и папку `images` в одну папку.
   - Дважды кликните по `update.exe` (откроется консоль).

2. **Отвечайте на вопросы по порядку:**
   - Программа покажет: "Заголовок главной страницы (По умолчанию: 'Веб-разработчик'): "
   - **Enter** – использует пример.
   - Или напишите своё (например: "Иван Иванов – будущий разработчик") и Enter.
   - Так для всего:
     - Описание, фото (`images/my_photo.jpg`), возраст (`21`), город (`Красноярск`).
     - Навыки: числа от 0 до 100, например `90` для HTML5.
     - Счетчики: `15` проектов, `10` клиентов.
     - **Проекты:** Спросит "Добавить проект #1? (y/n)": `y` – да, `n` – нет.
       - Фото: `images/project_1.jpg`
       - Название: "Мой первый сайт"
       - Описание: "Сайт для друзей"
       - Категория: `web`, `app` или `design`
       - Ссылка: `https://github.com/ваш-проект` или `#`
     - Контакты, соцсети.

3. **Готово!**
   - Программа скажет: "✅ Готово! Откройте update_index.html в браузере."
   - Дважды кликните `update_index.html` – откроется ваше портфолио.

## Хотите добавить/изменить?
Запустите `update.exe` заново – введите новые данные.

## 🖼️ Про фото и категории
- Фото: JPG, положите в `images/`. Можно URL (например, из Imgur).
- Категории проектов:
  - web: HTML, CSS, JS
  - app: React, Node
  - design: Карандаш, краски

## ❗ Если что-то не так?
- **Нет update_index.html?** Проверьте `index.html` существует.
- **Фото не видно?** Проверьте имена файлов в `images/`.
- **Программа не запускается?** Антивирус? Запустите от админа (правой кнопкой → "Запуск от имени администратора").
- **Ошибки?** Убедитесь, папка `images` есть.

**Теперь ваше портфolio для приёмной комиссии готово за 5 минут! 🚀 Покажите, что вы умеете!**
